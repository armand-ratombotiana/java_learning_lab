# Why It Exists: Distributed Cache

This capstone implements a distributed caching system similar to Redis/Memcached but built from scratch in Java. It demonstrates consistent hashing, cluster membership (gossip protocol), replication, cache eviction policies, and client-side routing. The goal is to understand the internals of distributed caching systems that are fundamental to scaling web applications.
