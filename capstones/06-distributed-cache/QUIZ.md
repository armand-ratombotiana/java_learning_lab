# Quiz: Distributed Cache

1. What problem does consistent hashing solve compared to modulo-N hashing?
2. How does the gossip protocol detect node failures?
3. What is the difference between write-through and write-behind caching?
4. How does LRU eviction track access recency?
5. What is the quorum condition for strong consistency (W + R > N)?
6. How do virtual nodes improve load distribution in consistent hashing?
7. What happens to cached data when a node joins the cluster?
8. How does TTL expiration work alongside LRU eviction?
9. What is split-brain and how do you prevent it?
10. How does client-side caching (near cache) reduce load on the distributed cache?
