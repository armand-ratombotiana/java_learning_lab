# Why It Exists: RAG Platform

This capstone implements a full RAG (Retrieval-Augmented Generation) platform that enables LLMs to answer questions based on custom knowledge bases. It demonstrates document ingestion, chunking, embedding, vector search, reranking, and LLM integration with context augmentation. The goal is to build the core infrastructure behind enterprise Q&A systems and AI knowledge assistants.
