# Quiz: Event Streaming

1. What is the purpose of partitions in a topic?
2. How does Kafka achieve high write throughput?
3. What is the ISR (In-Sync Replicas) and why does it matter?
4. How does consumer group rebalancing work?
5. What is log compaction and when would you use it?
6. How does the offset index improve read performance?
7. What is the difference between acks=0, acks=1, and acks=all?
8. How do producers decide which partition to write to?
9. What happens when a broker fails in a Kafka cluster?
10. How does Kafka provide exactly-once semantics?
