# Quiz: Vector Database

1. What is the difference between HNSW and IVF indexing?
2. Why is brute-force kNN impractical for large-scale vector search?
3. What does the M parameter in HNSW control?
4. How does product quantization reduce memory usage?
5. What is the curse of dimensionality?
6. How does metadata filtering interact with ANN search?
7. What is a WAL and why is it needed?
8. How does efSearch affect the recall-latency trade-off?
9. When would you choose cosine distance over L2 distance?
10. What are the advantages of memory-mapped files for vector storage?
