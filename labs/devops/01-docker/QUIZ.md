# Docker Quiz

1. What is the difference between an image and a container?
2. How does Docker achieve process isolation?
3. What is the purpose of a Dockerfile CMD vs ENTRYPOINT?
4. When would you use `COPY` instead of `ADD`?
5. How do multi-stage builds reduce image size?
6. What is the default Docker network driver?
7. How can you persist data when a container is deleted?
8. What does `docker system prune` do?
9. What is the OCI specification?
10. How does layer caching work in Docker builds?

Answers available in THEORY.md and CODE_DEEP_DIVE.md.
