# Visual Guide to CI/CD

## Pipeline Flow
```
┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│ SOURCE   │──▶│ BUILD    │──▶│ TEST     │──▶│ SECURITY │──▶│ PACKAGE  │──▶│ DEPLOY   │──▶│ VERIFY   │
│ Checkout │   │ Compile  │   │ Unit/Int │   │ SAST/SCA │   │ Docker   │   │ Staging  │   │ Smoke    │
└──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
                                       │
                                 ┌─────▼──────┐
                                 │ FAIL ❌    │
                                 │ Notify dev │
                                 └────────────┘
```

## GitHub Actions Workflow Structure
```
.github/workflows/ci.yml
│
push/PR ──▶ Trigger event
                │
                ▼
           ┌─────────┐
           │  Job 1  │───▶ Step: Checkout
           │  Build  │───▶ Step: npm ci
           └────┬────┘───▶ Step: npm test
                │
                ▼
           ┌──────────┐
           │  Job 2   │───▶ Step: Docker build
           │  Deploy  │───▶ Step: Push registry
           └──────────┘───▶ Step: kubectl apply
```

## DevOps Feedback Loop
```
Dev → Commit → Pipeline → Test Result
  ↑                           │
  └─────── Fix & Push ◄──────┘ (if fail)
```
