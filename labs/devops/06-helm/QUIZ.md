# Helm Quiz

1. What is the difference between a Chart and a Release?
2. What is the purpose of values.yaml?
3. How does the three-way merge strategy work in Helm 3?
4. What is the difference between `helm template` and `helm install --dry-run`?
5. How do you manage chart dependencies?
6. What is a Helm hook and when would you use one?
7. How do you override values from the command line?
8. What is the purpose of _helpers.tpl?
9. How does Helm store release information?
10. What are the advantages of Helm 3 over Helm 2?

Answers available in THEORY.md and CODE_DEEP_DIVE.md.
