# INTERVIEW — Reflection Basics

## Company-Specific Focus

### Google
- How does `Class.forName()` differ from `obj.getClass()`?
- Reflection performance overhead — when is it acceptable?

### Amazon
- Using reflection in DI containers (Spring, Guice)
- `setAccessible()` security implications

### Meta
- Reflection for serialization frameworks
- Module‑system access restrictions in Java 9+

### Oracle
- `MethodHandle` vs `java.lang.reflect` — performance comparison
- `VarHandle` for field access

## Common Questions
1. Can you reflect on a lambda?
2. How does `isAccessible()` relate to modules?
3. What is the cost of `setAccessible(true)`?
