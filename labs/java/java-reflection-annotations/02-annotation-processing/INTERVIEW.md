# INTERVIEW — Annotation Processing

## Company-Specific Focus

### Google
- RetentionPolicy.CLASS vs RUNTIME — use cases
- How Lombok implements `@Data` via annotation processors

### Amazon
- Custom annotations for request validation in REST APIs
- `@Validated`, `@NotNull` — runtime vs compile‑time

### Oracle
- `@Repeatable` annotations — container annotation pattern
- `@Deprecated` evolution (forRemoval, since)

## Common Questions
1. Can annotations extend other annotations?
2. How does `@Inherited` work?
3. What is the `ElementType.TYPE_USE` for?
