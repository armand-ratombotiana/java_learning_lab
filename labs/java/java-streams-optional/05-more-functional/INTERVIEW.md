# INTERVIEW — More Functional

## Company-Specific Focus

### Google
- Function composition over imperative loops
- `java.util.function` — 43 functional interfaces, which ones do you know?

### Amazon
- Functional pipelines for order processing
- Memoization for expensive computations (pricing rules, ML inference)

### Oracle
- `@FunctionalInterface` annotation — compile‑time checking
- `Function.identity()`, `IntFunction` primitive specializations

## Common Questions
1. Why is there no `TriFunction` in `java.util.function`?
2. How does `andThen()` differ from `compose()`?
3. What is the memory cost of memoization?
