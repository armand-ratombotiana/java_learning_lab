# Quiz — Reflection

1. How do you obtain a `Class<?>` object?
2. What is the difference between `getMethods()` and `getDeclaredMethods()`?
3. How do you invoke a private method?
4. What does `setAccessible(true)` do?
5. What is `InvocationTargetException`?
6. How do you create a dynamic proxy?
7. What is the role of `InvocationHandler`?
8. What is `MethodHandle` and how is it different from `Method`?
9. Why does `Class.forName()` throw `ClassNotFoundException`?
10. What is the Java 9+ module restriction on reflection?

**Answers** — refer to theory files.
