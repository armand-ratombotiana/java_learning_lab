# Quiz — Concurrency

1. What is the difference between a process and a thread?
2. How does `synchronized` work at the JVM level?
3. What is a race condition?
4. Name three ways to create a thread in Java.
5. What is Amdahl's Law?
6. What is the difference between `submit()` and `execute()` on ExecutorService?
7. What is a `ReentrantLock` and how is it different from `synchronized`?
8. What is happen-before?
9. How does `CompletableFuture.allOf` differ from `anyOf`?
10. What is a deadlock? List the four Coffman conditions.

**Answers** — refer to theory files.
