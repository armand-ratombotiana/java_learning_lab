# Quiz — I/O & NIO

1. What is the difference between `InputStream` and `Reader`?
2. What does `BufferedInputStream` do internally?
3. What is the purpose of try-with-resources for I/O?
4. What is the difference between `File` and `Path`?
5. How does `FileChannel.transferTo()` achieve zero-copy?
6. What is the difference between a direct and heap `ByteBuffer`?
7. What is a `Selector` used for?
8. What does `Files.walk()` return?
9. What is a `WatchService`?
10. How do you memory-map a file in Java?

**Answers** — refer to theory files.
