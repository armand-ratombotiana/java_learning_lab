# Quiz — Java Streams

1. What distinguishes intermediate from terminal operations?
2. Can a stream be consumed twice?
3. What does `map()` vs `flatMap()` do?
4. How is a parallel stream's default thread pool configured?
5. What is a `Spliterator`?
6. When would `reduce()` be preferred over `collect()`?
7. Name three short-circuiting operations.
8. How do you convert `Stream<Integer>` to `IntStream`?
9. What does `Collectors.toUnmodifiableList()` guarantee?
10. Why are streams lazy?

**Answers** — refer to theory files.
