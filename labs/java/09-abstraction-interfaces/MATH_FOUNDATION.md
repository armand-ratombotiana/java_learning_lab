# Math Foundation for Abstraction & Interfaces

No specific math foundation required. Abstraction is a conceptual tool — the ability to reason about "what" vs "how" is logical, not mathematical.

Understanding contracts (interfaces define what a caller can expect) is a design skill, not a math skill.
