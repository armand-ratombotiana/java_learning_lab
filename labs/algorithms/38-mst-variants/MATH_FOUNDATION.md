# Math Foundation
Boruvka: each component merges at least once per round, so O(log V) rounds. Steiner 2-approx: cost(MST on terminals) <= 2 * opt. MBST: MST is always MBST. Euclidean MST: Delaunay triangulation contains MST edges.
