# Theory
Boruvka's algorithm: repeatedly adds cheapest edge from each component. Runs O(E log V). Steiner tree: connects terminals optionally via non-terminal vertices; 2-approx via terminal-pair MST. MBST: minimizes maximum edge weight; every MST is MBST. Euclidean MST: MST in 2D plane where edge weight = Euclidean distance.
