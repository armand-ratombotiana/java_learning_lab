# Common Mistakes
Boruvka: not excluding edges within the same component. Steiner: running MST on original graph instead of metric closure. MBST: using MST algorithm instead of binary search. Euclidean: considering all O(n^2) edges instead of pruning.
