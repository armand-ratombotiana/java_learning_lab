# Why It Exists
Boruvka parallelizes naturally (each component's cheapest edge found independently). Steiner tree models network design (roads, cables) with intermediate junctions. MBST is used in clustering and network design where maximum latency matters. Euclidean MST analyzes spatial point patterns.
