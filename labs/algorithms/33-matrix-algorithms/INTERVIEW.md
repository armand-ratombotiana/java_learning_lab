# Interview Questions — Matrix Algorithms

1. Multiply two matrices using Strassen's algorithm (hard, specialized)
2. Implement Gaussian elimination (scientific computing roles)
3. Compute the determinant of a matrix (general SWE roles)
4. How does LU decomposition speed up repeated solves?
5. Explain SVD and its applications (ML/data science roles)
6. How does power iteration compute PageRank?
7. What is numerical stability and why does pivoting matter?
8. Compute the matrix inverse (Apple, NVIDIA)
9. What is the condition number of a matrix?
10. Implement matrix multiplication with cache-friendly blocking