# Math Foundation
SAM size <= 2n-1, transitions <= 3n-4. Manacher O(n) because each expansion extends right bound. BWT LF mapping: last-to-first column correspondence. Huffman optimality: prefix code minimizing sum(freq_i * len_i).
