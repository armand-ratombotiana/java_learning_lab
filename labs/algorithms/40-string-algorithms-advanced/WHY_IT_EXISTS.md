# Why It Exists
Suffix Automaton solves substring queries (checking, counting distinct substrings) in O(|S|). Manacher is optimal for palindrome problems. BWT improves compression by grouping similar characters. Huffman provides optimal prefix-free codes.
