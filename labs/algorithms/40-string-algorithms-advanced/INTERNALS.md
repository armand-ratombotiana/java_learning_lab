# Internals
SAM states have next[26] array and link pointer. Clone state copies transitions. Manacher uses ^ and $ sentinels to avoid bounds checking. BWT inverses uses next array from counting sort. Huffman uses PriorityQueue.
