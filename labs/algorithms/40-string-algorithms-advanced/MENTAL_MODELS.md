# Mental Models
SAM: a road map where each path spells a substring. Manacher: mirrors reflecting palindromes. BWT: a reversible text shuffler that clusters letters. Huffman: building a tree by repeatedly merging the two least frequent nodes.
