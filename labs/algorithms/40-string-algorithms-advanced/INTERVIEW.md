# Interview
Q: Longest palindrome in O(n). A: Manacher's algorithm. Q: Substring check in O(|S|). A: Suffix automaton or suffix tree. Q: Design a compression algorithm. A: BWT + move-to-front + Huffman (bzip2).
