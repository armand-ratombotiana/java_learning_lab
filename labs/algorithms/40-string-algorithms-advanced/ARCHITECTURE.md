# Architecture
4 source files, 1 test. SuffixAutomaton uses State inner class. Manacher is static utility with preprocess. BurrowsWheeler has transform/inverse paired. HuffmanCoding has build/encode/decode lifecycle.
