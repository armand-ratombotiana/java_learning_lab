# Interview Questions — Suffix Array

1. Find the longest repeated substring in a string (Amazon)
2. Count the number of distinct substrings (Google)
3. Find the longest common prefix of two strings
4. Search a pattern in a text using suffix array
5. Build the Burrows-Wheeler transform from a suffix array
6. Find the shortest unique substring covering a given position
7. Merge two suffix arrays (for concatenated strings)
8. Implement the prefix-doubling algorithm from scratch
9. Compute the LCP array using Kasai's algorithm
10. Find the longest palindrome using suffix arrays and LCP