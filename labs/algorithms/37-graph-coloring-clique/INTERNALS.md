# Internals
Adjacency matrix for O(1) edge checks. DSatur maintains saturation counters updated incrementally when a vertex is colored. Bron-Kerbosch uses recursion with pivot selection to prune branches. Maximum clique uses branch-and-bound with a greedy coloring bound (Carraghan-Pardalos).
