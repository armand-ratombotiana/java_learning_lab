# Performance
Greedy: O(V^2). Welsh-Powell: O(V log V + E). DSatur: O(V^2) naive, O((V+E) log V) with heap. Bron-Kerbosch: O(3^(n/3)) worst-case (optimal). Maximum clique branch-and-bound: exponential but practical for graphs up to ~100 vertices.
