# Theory
Graph Coloring: assign colors to vertices such that no adjacent vertices share a color. The chromatic number is the minimum colors needed. Greedy coloring processes vertices in arbitrary order, assigning the smallest available color. Welsh-Powell sorts by descending degree first. DSatur dynamically selects the vertex with highest saturation degree.
