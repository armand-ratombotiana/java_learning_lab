# Architecture: PostgreSQL

## Single Server Architecture
```
┌─────────────────────────────────────┐
│         PostgreSQL Server            │
├─────────────────────────────────────┤
│  ┌─────────┐  ┌──────────────────┐  │
│  │ Postmaster│  │ Shared Memory    │  │
│  │  Process  │  │ ┌──────┐┌─────┐ │  │
│  └────┬─────┘  │ │Shared││WAL   │ │  │
│       │        │ │Buffers││Buffer│ │  │
│  ┌────┴─────┐  │ └──────┘└─────┘ │  │
│  │ Backend  │  └──────────────────┘  │
│  │ Processes │                       │
│  │ ┌──┐┌──┐ │                       │
│  │ │P1││P2│ │                       │
│  │ └──┘└──┘ │                       │
│  └──────────┘                       │
│  ┌───────────────┐                  │
│  │ WAL Writer    │  ┌──────────┐   │
│  │ Checkpointer  │  │ Data Files│   │
│  │ Autovacuum    │  │ WAL Files │   │
│  │ Stats Worker  │  │ Archives  │   │
│  └───────────────┘  └──────────┘   │
└─────────────────────────────────────┘
```

## Replication Architecture (Streaming)
```
┌──────────┐     WAL Stream     ┌──────────┐
│ Primary  │──────────────────►│ Standby   │
│          │                    │  (Hot)    │
│ WAL ─────┘                    └───── WAL  │
│ Send                          │ Apply     │
│                    ┌──────────┐│ ┌────────┐│
│                    │ Read Replica│ │ (RO)   │
│                    │ Queries    │ │         │
│                    └──────────┘│ └────────┘│
└──────────┘                    └──────────┘
```

## Patroni HA Architecture
```
┌───────────────────────────┐
│       etcd / Consul       │  ← Distributed consensus
└──────────┬────────────────┘
           │
    ┌──────┴──────┐
    │   Patroni   │  ← HA manager
    └──────┬──────┘
           │
    ┌──────┴──────┴──────┐
    │         │          │
┌───┴───┐ ┌───┴───┐ ┌───┴───┐
│Primary│ │Standby│ │Standby│
│  PG   │ │  PG   │ │  PG   │
└───────┘ └───────┘ └───────┘
```

## Application Integration (Spring Boot)
```
┌──────────────┐     ┌──────────┐     ┌──────────┐
│  App Service  │────►│HikariCP  │────►│PostgreSQL│
│  Spring Boot  │     │  Pool    │     │ Primary  │
└──────────────┘     └──────────┘     └──────────┘
        │                                    │
        │                              ┌─────┴─────┐
        │                              │ PostgreSQL │
        └─────────────────────────────►│ Replica(s) │
                                       │ (read-only)│
                                       └───────────┘
```

## Multi-Tenant Architectures
1. **Database per tenant**: Complete isolation, complex operations
2. **Schema per tenant**: Moderate isolation, shared connection pool
3. **Row-level security**: Shared tables, RLS policies
