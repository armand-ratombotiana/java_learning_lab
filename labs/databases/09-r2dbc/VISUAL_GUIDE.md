# Visual Guide: R2DBC Reactive Flow

```
Thread Model Comparison:

JDBC (Blocking):
┌─────────┐   ┌─────────┐   ┌─────────┐
│ Thread 1 │──→│ DB Call  │──→│ Blocked │
│ (Handler)│   │         │   │ (wait)  │
└─────────┘   └─────────┘   └─────────┘
┌─────────┐   ┌─────────┐   ┌─────────┐
│ Thread 2 │──→│ DB Call  │──→│ Blocked │
│ (Handler)│   │         │   │ (wait)  │
└─────────┘   └─────────┘   └─────────┘
         ... thread per connection ...

R2DBC (Non-blocking):
┌─────────────┐
│ Event Loop  │──→ Query ──→ ──→ Callback → onNext()
│ (1-4 threads)│──→ Query ──→ ──→ Callback → onNext()
│             │──→ Query ──→ ──→ Callback → onNext()
└─────────────┘
         ... multiplexed over few threads ...

Backpressure Flow:
┌──────────┐   request(3)    ┌──────────┐
│ Consumer │────────────────→│ Producer │
│          │←─── onNext ─────│ (Driver) │
│          │←─── onNext ─────│          │
│          │←─── onNext ─────│          │
│          │   request(2)    │          │
│          │────────────────→│          │
│          │←─── onNext ─────│          │
│          │←─── onNext ─────│          │
└──────────┘                 └──────────┘
```
