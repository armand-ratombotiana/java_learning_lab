# Refactoring: GraphQL DGS

Before: Multiple REST endpoints for related data
After: Single GraphQL query with DataLoader batching

Before: Over-fetching via REST
After: Precise field selection via GraphQL
