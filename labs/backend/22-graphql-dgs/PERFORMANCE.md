# Performance: GraphQL DGS

- DataLoaders reduce N+1 queries significantly
- Use pagination for list queries
- Limit query depth and complexity
- Enable batching for efficient DB access
- Cache frequently accessed data
- Use Query Complexity Analysis to prevent expensive queries
- Monitor resolver execution times
- Consider persisted queries for production
