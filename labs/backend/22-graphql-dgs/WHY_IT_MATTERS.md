# Why It Matters: GraphQL DGS

GraphQL solves the over-fetching and under-fetching problems of REST APIs. DGS provides a production-tested framework with built-in support for federation, subscriptions, and DataLoaders.
