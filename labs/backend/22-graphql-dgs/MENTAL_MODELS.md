# Mental Models: GraphQL DGS

The **Menu-Like Order Model**: A GraphQL query is like ordering from a menu - you specify exactly what you want (fields). The server is the kitchen that prepares only what you ordered. DataLoaders are like bulk-preparing ingredients for all orders at once.
