# Why REST APIs Exist

Before REST, SOAP and XML-RPC dominated. REST emerged to provide:
- Simpler, HTTP-native communication
- Resource-oriented design (not RPC)
- Scalable stateless architecture
- Multiple data formats (JSON, XML, etc.)
- Cacheability for performance

REST made APIs accessible to web developers without specialized tooling.
