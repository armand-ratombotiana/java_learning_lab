# Mental Models: Multi-Tenancy

The **Apartment Building Model**: Database-per-tenant is individual houses (complete isolation). Schema-per-tenant is separate units in one building (shared walls). Discriminator column is a single room with cubicles (shared space with dividers).
