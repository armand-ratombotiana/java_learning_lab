# Mental Models: Performance

The **Water Pipe Model**: Throughput is pipe diameter, latency is pipe length. Caching is like having reservoirs closer to consumers. Connection pooling is like having multiple parallel pipes. GC pauses are like pipe cleaning interruptions.
