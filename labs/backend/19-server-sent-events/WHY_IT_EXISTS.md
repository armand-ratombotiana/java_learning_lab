# Why It Exists: SSE

Before SSE, real-time updates required polling (wasteful) or WebSocket (complex infrastructure). SSE provides a middle ground - simple, HTTP-native, with browser auto-reconnection.
