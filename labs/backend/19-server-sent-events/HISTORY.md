# History: SSE

SSE was introduced by Opera in 2006 as part of HTML5. It was standardized by the WHATWG in the HTML specification. Despite being simpler than WebSocket, it has lower adoption due to browser limitations and the popularity of WebSocket.
