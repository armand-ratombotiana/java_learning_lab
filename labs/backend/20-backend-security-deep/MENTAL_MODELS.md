# Mental Models: Security

The **Castle Model**: Multiple layers of defense. Outer wall (rate limiting), moat (CORS), guards (authentication), inner doors (authorization), treasure vault (database). Each layer must be breached individually.
