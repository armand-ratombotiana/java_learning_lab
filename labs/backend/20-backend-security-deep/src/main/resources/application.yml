spring:
  application:
    name: security-deep-lab
  datasource:
    url: jdbc:h2:mem:secdb
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: false

server:
  port: 8080

logging:
  level:
    com.learning.backend20: DEBUG
    org.springframework.security: INFO
