# Why It Exists: API Versioning & Documentation

API versioning exists because APIs evolve independently of their consumers. You need to improve your API without breaking existing integrations. Documentation exists because APIs are consumed by developers who need to understand the interface without reading source code. Without explicit versioning and documentation, API maintenance becomes chaotic.
