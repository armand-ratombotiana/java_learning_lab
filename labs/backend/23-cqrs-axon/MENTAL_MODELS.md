# Mental Models: CQRS Axon

The **Bank Ledger Model**: Commands are like writing checks (actions). Events are like journal entries (facts). The aggregate is the running balance (recalculated from all entries). Projections are monthly statements (precomputed views).
