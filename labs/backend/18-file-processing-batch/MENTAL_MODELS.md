# Mental Models: Spring Batch

The **Assembly Line Model**: Items move down a conveyor belt. Workers (processors) modify items. At the end, items are packed into boxes (chunks) and shipped (written). If a defective item is found, it's removed (skipped) without stopping the line.
