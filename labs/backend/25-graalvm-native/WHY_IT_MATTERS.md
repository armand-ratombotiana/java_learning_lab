# Why It Matters: GraalVM Native

Native images enable instant startup (critical for serverless, container orchestration, CI/CD), lower memory usage (reducing cloud costs), and smaller deployable artifacts (faster deployments).
