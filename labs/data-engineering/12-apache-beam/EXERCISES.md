# Exercises for Apache Beam

1. Build word count pipeline with ParDo, Count, and TextIO
2. Create streaming pipeline with KafkaIO, windowing, and trigger configuration
3. Implement stateful DoFn for sessionization of user events
4. Use side inputs for enrichment from external data source
5. Create custom composite transform combining multiple PTransforms
6. Run the same pipeline on DirectRunner and FlinkRunner
