# Visual Guide to AWS Database

## 1. AWS Database Services Overview

```
                    ┌─────────────────────────────┐
                    │    Relational (SQL)          │
                    │  ┌─────┐ ┌──────┐           │
                    │  │ RDS │ │Aurora│           │
                    │  └─────┘ └──────┘           │
                    └─────────────────────────────┘

                    ┌─────────────────────────────┐
                    │    NoSQL (Key-Value/Doc)     │
                    │  ┌──────────┐               │
                    │  │ DynamoDB │               │
                    │  └──────────┘               │
                    └─────────────────────────────┘

                    ┌─────────────────────────────┐
                    │    In-Memory Cache           │
                    │  ┌────────────┐             │
                    │  │ElastiCache│             │
                    │  │(Redis/Mem)│             │
                    │  └────────────┘             │
                    └─────────────────────────────┘

                    ┌─────────────────────────────┐
                    │    Data Warehouse           │
                    │  ┌──────────┐               │
                    │  │ Redshift │               │
                    │  └──────────┘               │
                    └─────────────────────────────┘
```

## 2. DynamoDB Table Structure

```
Table: Orders
  ├── Partition Key: orderId (String)
  ├── Sort Key: createdAt (Number)
  └── Attributes:
      ├── customerId (String)
      ├── amount (Number)
      ├── status (String)
      ├── items (List)
      └── shippingAddress (Map)

GSI: CustomerIndex
  ├── Partition Key: customerId
  └── Sort Key: createdAt

LSI: StatusIndex
  ├── Partition Key: orderId
  └── Sort Key: status
```

## 3. RDS Multi-AZ Failover

```
Normal:
  ┌────────┐          ┌────────┐
  │Primary │─────────►│Standby │
  │us-e-1a │  sync    │us-e-1b │
  └───┬────┘          └────────┘
      │
  App writes to
  myapp.xxx.rds.amazonaws.com

Failover:
  ┌────────┐          ┌────────┐
  │Primary │  OUTAGE  │Standby │
  │us-e-1a │          │us-e-1b │
  └────────┘          └───┬────┘
                          │
                     Promoted to
                     PRIMARY
                          │
                  App writes to
                  myapp.xxx.rds.amazonaws.com
                  (DNS updated ~60s)
```

## 4. Aurora Cluster Architecture

```
                            ┌──────────────┐
                            │  Endpoint     │
                            │  myapp.cluster│
                            │  xxx.rds...   │
                            └──────┬───────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                    │
         ┌────▼────┐        ┌─────▼─────┐       ┌─────▼─────┐
         │ Writer   │        │ Reader 1  │       │ Reader 2  │
         │(primary) │        │(replica)  │       │(replica)  │
         └────┬─────┘        └─────┬─────┘       └─────┬─────┘
              │                    │                    │
              └────────────────────┼────────────────────┘
                                   │
                        ┌──────────▼──────────┐
                        │  Shared Storage      │
                        │  (6 copies, 3 AZs)   │
                        │  Auto-scaling 128TB  │
                        └─────────────────────┘
```
