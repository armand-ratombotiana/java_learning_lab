# Mini Project: Dynamic Range Statistics

Build a tool that maintains an array and supports dynamic range statistics (sum, min, max, average) using segment trees.

## Features
- Real-time updates to array elements
- Range sum, min, max, average queries
- Visualization of the segment tree structure
- Batch updates with lazy propagation
