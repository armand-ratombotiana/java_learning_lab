# Benchmark: Spatial Query Performance

Compare quadtree vs k-d tree vs brute force for:
- Nearest neighbor search (varying point counts)
- Range search (varying query sizes)
- Construction time
- Memory usage
