# Mini Project: GIS Point of Interest Finder

Build a spatial index for points of interest (restaurants, gas stations).
- Add locations with coordinates
- Find nearest POI to a given location
- Find all POIs within a radius
