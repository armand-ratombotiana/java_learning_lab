# Mini Project: In-Memory Key-Value Store

Build a fast in-memory key-value store using skip lists.

## Features
- Put, get, remove operations
- Range queries (get all keys in [low, high])
- Ordered iteration
- Persistence to disk (serialization)
